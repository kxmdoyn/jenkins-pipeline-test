apiVersion: v1
kind: Service
metadata:
  name: jenkins-pipeline-test
  namespace: class-2
spec:
  selector:
    app: jenkins-pipeline-test
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
